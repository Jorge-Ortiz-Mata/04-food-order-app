import React from 'react';
import { render, screen } from '@testing-library/react';
import App from './App';

// test('see Nav Bar title', () => {
//   render(<App />);
//   const linkElement = screen.getByText('Food Order App');
//   expect(linkElement).toBeInTheDocument();
// });
